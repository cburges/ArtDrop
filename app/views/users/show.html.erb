<h2> <%= @user.first_name %> <%= @user.last_name %> </h2>

<h2>Projects:</h2>
<% @user.projects.each do |project| %>
  <p>
    <strong>
      <%= project.title %>
    </strong>
  </p>

  <p>
    <strong>
      <%= project.description %>
    </strong>
  </p>
<% end %>
